export interface Item {
    id: number;
    name: string;
    pricePerNight: number;
    fromDate: Date;
    toDate: Date;
    capacity: number;
    description: string;
    location: string;
    imageUrl: string;
}

interface Order {
    
}

interface ItemAvailability {
    
}
